[package]
name = "openbook"
version = "0.1.0"
description = "OpenBook — launcher plein écran pour Windows"
authors = ["GraineOS"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "openbook_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
